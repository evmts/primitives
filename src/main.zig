const std = @import("std");
const primitives = @import("root.zig");

pub fn main() !void {
    std.debug.print("Ethereum Primitives Library\n", .{});
    std.debug.print("Run `zig build test` to run the tests.\n", .{});
}
